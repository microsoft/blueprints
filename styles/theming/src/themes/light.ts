import { breakpoints } from '../breakpoints';
import { layout } from '../layout';

export const light = {
  color: {
    overlay: {
      background: 'rgb(255 255 255 / 75%)',
    },
  },
  shape: {
    ['border-radius']: '1rem',
  },
  breakpoints,
  layout,
};
