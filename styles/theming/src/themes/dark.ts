import { breakpoints } from '../breakpoints';
import { layout } from '../layout';

export const dark = {
  color: {
    overlay: {
      background: 'rgb(0 0 0 / 75%)',
    },
  },
  shape: {
    borderRadius: '1rem',
  },
  breakpoints,
  layout,
};
