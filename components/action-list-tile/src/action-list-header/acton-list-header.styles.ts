import { makeStyles } from '@griffel/react';

export const useActionListTileStyles = makeStyles({
  image: {
    height: 'var(--space-8)',
    width: 'var(--space-8)',
    objectFit: 'cover',
  },
});
