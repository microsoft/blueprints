export type DemoComponentProps = {
  /**
   * Custom class name for the component root.
   * @default undefined
   */
  className?: string;

  /**
   * The text of the component.
   * @default 'Hello'
   */
  text?: string;
};
