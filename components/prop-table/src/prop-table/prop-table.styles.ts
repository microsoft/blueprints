import { tokens } from '@fluentui/react-theme';
import { makeStyles } from '@griffel/react';

export const usePropTableStyles = makeStyles({
  root: {
    color: tokens.colorNeutralForeground1,
  },
});
